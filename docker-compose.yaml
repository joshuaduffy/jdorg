version: '3'
services:
  web:
    image: nginx
    volumes:
      - ./build:/usr/share/nginx/html
    ports:
      - "8080:80"
    environment:
      - NGINX_PORT=80
    networks:
      - web

networks:
  web:
    driver: bridge
